# @dyktig/supabase

Delt Supabase-pakke for monorepoet.

- `createSupabaseBrowserClient()` — Client Components
- `getSupabaseEnv()` — miljøvariabler
- `Database` — genererte Postgres-typer

Server-klient: `apps/web/src/lib/supabase/server.ts`
